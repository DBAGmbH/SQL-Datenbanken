CREATE TABLE Filme
(
   Titel VARCHAR (100),
   Genre VARCHAR (30),
   USK INTEGER,
   Regisseur VARCHAR (30),
   Laenge INTEGER,
   Veroeffentlichung DATE
);

CREATE TABLE Filme
(
   Titel VARCHAR (100),
   Genre VARCHAR (30),
   USK INTEGER,
   Regisseur VARCHAR (30),
   Laenge INTEGER,
   Veroeffentlichung DATE
);

CREATE TABLE Filme
(
   Titel VARCHAR (100),
   Genre VARCHAR (30),
   USK INTEGER,
   Regisseur VARCHAR (30),
   Laenge INTEGER,
   Veroeffentlichung DATE
);

CREATE TABLE Filme
(
   Titel VARCHAR (100),
   Genre VARCHAR (30),
   USK INTEGER,
   Regisseur VARCHAR (30),
   Laenge INTEGER,
   Veroeffentlichung DATE
);


CREATE TABLE Filme
(
   Titel VARCHAR (100),
   Genre VARCHAR (30),
   USK INTEGER,
   Regisseur VARCHAR (30),
   Laenge INTEGER,
   Veroeffentlichung DATE
);





































